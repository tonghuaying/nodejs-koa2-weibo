<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>EJS Welcome to <%= title %></p>
    <p><%= message %></p>
    <!-- 不确定是否有 name 这个变量，在前面加 locals -->
    <p><%= locals.name %></p>
    <hr />
    <!-- 引用组件 -->
    <%- include('widgets/user-info', {
      isMe
    })%>
    <hr />
    <%- include('widgets/blog-list.ejs', {
      blogList
    })%>
  </body>
</html>
